<% getGlobalImports().forEach((globalImport) => { -%>
<%- globalImport %>
<% }); -%>
<% getTypeExtraImports().forEach((line) => { -%>
<%- line %>
<% }); -%>
<% if (!utils.isPresentableEntity(schema) || schema.types.some(utils.isCustomType)) { -%>
  <%_ schema.types.filter(utils.isPresentableEntityType).filter((type) => type.name !== schema.entityName).forEach((type) => { -%>
import { <%= type.strings.fieldRequestClass %>, <%= type.strings.responseClass %> } from './<%= type.strings.typePackage %>';
  <%_ }); -%>
  <%_ schema.types.filter(utils.isImportType).forEach((type) => { -%>
import { <%= type.strings.externalClass %> } from '<%= type.strings.externalPackage %>';
  <%_ }); -%>
<% } -%>
<% if (schema.types.some(utils.isEnumType)) { -%>

/** enums */
  <%_ schema.types.filter(utils.isEnumType).forEach((type) => { -%>

    <%_ if (type.deprecated) { -%>
/** @deprecated */
    <%_ } -%>
export enum <%= type.name %> <%- type.strings.typeDefinition %>;
  <%_ }); -%>
<% } -%>
<% if (schema.types.some(utils.isDefineType)) { -%>

/** structs */
  <%_ schema.types.filter(utils.isDefineType).forEach((type) => { -%>

    <%_ if (type.deprecated) { -%>
/** @deprecated */
    <%_ } -%>
export type <%= type.name %> = <%- type.strings.typeDefinition %>;
  <%_ }); -%>
<% } -%>
<% if (!schema.internal) { -%>

  <%_ if (schema.deprecated) { -%>
/** @deprecated */
  <%_ } -%>
export type <%= schema.strings.fieldRequestClass %> = {
  <%_ schema.fields.filter(utils.isPresentableField).forEach((field) => { -%>
    <%_ if (field.deprecated) { -%>
  /** @deprecated */
    <%_ } -%>
  <%= field.name %>?: <%= field.strings.fieldRequestType %>;
  <%_ }); -%>
};

  <%_ if (schema.deprecated) { -%>
/** @deprecated */
  <%_ } -%>
export type <%= schema.strings.responseClass %> = {
  <%_ schema.fields.filter(utils.isPresentableField).forEach((field) => { -%>
    <%_ if (field.deprecated) { -%>
  /** @deprecated */
    <%_ } -%>
  <%= field.name %>?: <%= field.strings.fieldResponseType %>;
  <%_ }); -%>
};
<% } -%>
<% getTypeExtraLines().forEach((line) => { -%>
<%- line %>
<% }); -%>
